import { initializeApp } from 'firebase/app'; import { getFirestore, collection, getDocs, query, orderBy, limit } from 'firebase/firestore'; import { firebaseConfig } from './src/lib/firebase'; const app = initializeApp(firebaseConfig); const db = getFirestore(app); async function check() { const q = query(collection(db, 'jobs'), orderBy('createdAt', 'desc'), limit(1)); const snap = await getDocs(q); snap.forEach(d => console.log(JSON.stringify(d.data().insurance, null, 2))); process.exit(0); } check();
